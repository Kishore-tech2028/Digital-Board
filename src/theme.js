// src/theme.js
const theme = {
  colors: {
    // Brand Colors
    primary: '#0f766e', // A rich teal
    secondary: '#0e7490', // A strong cyan
    urgent: '#e11d48', // A vibrant rose for errors/pins

    // Neutral Colors
    white: '#ffffff',
    textMain: '#1e293b',   // Dark slate (for titles/text)
    textLight: '#64748b',  // Lighter slate (for subtitles)
    border: '#e2e8f0',     // Light gray (for borders/dividers)
    
    // Background/Surface Colors
    bgLight: '#f8fafc',    // Very light gray (for page backgrounds)
    surface: '#ffffff',   // White (for cards)

    // Old keys (for compatibility, you can remove them later)
    primary_old: "#0f172a", 
    secondary_old: "#6b7280",
  },
};

export default theme;